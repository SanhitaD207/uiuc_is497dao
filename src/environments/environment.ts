// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebaseConfig: {
        apiKey: "AIzaSyCzsK43Wm1QRcaH6Lh4md2IeybOanpIPfk",
        authDomain: "uiuc-is497dao.firebaseapp.com",
        databaseURL: "https://uiuc-is497dao.firebaseio.com",
        projectId: "uiuc-is497dao",
        storageBucket: "uiuc-is497dao.appspot.com",
        messagingSenderId: "17353544176",
        appId: "1:17353544176:web:761f696f6c035b4816cd35"
    }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
